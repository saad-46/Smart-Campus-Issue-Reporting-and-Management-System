rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow all authenticated users to read and write for the hackathon
    // WARNING: This is highly insecure and should be restricted before production deployment.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
  }
}
